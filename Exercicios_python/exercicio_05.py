'''
Objetivo:
Escreva um programa em Python que conte o número de vogais (a, e, i, o, u) em uma string fornecida pelo usuário.

Instruções:

Solicite ao usuário que insira uma string.
Crie um loop para percorrer cada caractere da string.
Conte quantas vezes cada vogal aparece na string.
Exiba o número de vezes que cada vogal aparece.
Dica:
Você pode usar um dicionário para armazenar a contagem de cada vogal e iterar sobre a string usando um loop for.

Exemplo de Saída
Para a string "exemplo de string", a saída deve ser algo como:

codigo
A vogal 'a' aparece 0 vezes.
A vogal 'e' aparece 2 vezes.
A vogal 'i' aparece 2 vezes.
A vogal 'o' aparece 2 vezes.
A vogal 'u' aparece 0 vezes.
'''